{
  "name": "scalejs.metadatafactory",
  "main": "dist/scalejs.metadataFactory.js",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir dist --source-maps && npm run copy",
    "copy": "copyup src/**/*.html src/**/*.css dist"
  },
  "version": "0.1.9",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "copyfiles": "^0.2.1",
    "rimraf": "^2.5.2"
  },
  "dependencies": {
    "knockout": "^3.4.0",
    "lodash": "^4.13.1",
    "moment": "^2.13.0",
    "scalejs": ">=0.4.1",
    "scalejs.expression-jsep": ">=0.0.3"
  }
}
